server:
  grpc_port: "50051"
  http_port: "8080"
  host: "0.0.0.0"

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "postgres"
  database: "users"
  ssl_mode: "disable"

logger:
  level: "info"
  format: "json"
